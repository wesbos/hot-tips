const tops = ['sauce', 'cheese', 'pepperoni', 'sausage', 'peppers', 'onions', 'mushrooms', 'olives'];


tops.at(0)
tops.at(1)

tops[tops.length - 1]
tops.at(-1)
tops.at(-2)

tops[Math.floor(Math.random() * tops.length)]

tops.at(Math.random() * tops.length)

